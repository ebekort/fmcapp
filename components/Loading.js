import {View, Text} from 'react-native'

function Loading( navigation ) {
    return (
        <View>
            <Text>
                Loading...
            </Text>
        </View>
    );
}

export default Loading